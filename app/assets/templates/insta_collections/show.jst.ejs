<h1>Hey there</h1>

<ul>
<% for (var i = 0; i < instaCollection.posts().length; i++) { %>
  <% var post = instaCollection.posts().models[i] %>
  <li>
  <% if (post.escape("media_type") === "image") { %>
    <img src="<%= post.escape("media_url") %>"/>
  <% } else { %>
    <video controls autoplay name="media">
      <source src="<%= post.escape("media_url") %>" type="video/mp4">
    </video>
  <% } %>
  </li>
<% } %>
</ul>
